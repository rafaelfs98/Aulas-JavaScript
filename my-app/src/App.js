import "./App.css";
import Header from "./Header";

function App() {
  return (
    <Header
      name="RafaelDev"
      links={["Sobre", "Comprar", "Contato", "Login"]}
    ></Header>
  );
}

export default App;
